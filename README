David Voronov
Lab Section: 3pm

Design Decisions:

A FLAG = 9 (response to filename from server) is good if the payload starts with the integer 0, is bad if it has the integer 1.

I used FLAG = 33 to say this pdu is valid. This is used to check if a pdu contains valid data when attempting to read a pdu from the window library.
